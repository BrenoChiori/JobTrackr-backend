package jobtrackr.Repository;

import jobtrackr.Entity.VagaCandidatada;
import org.springframework.data.jpa.repository.JpaRepository;

public interface VagaCandidatadaRepository extends JpaRepository<VagaCandidatada, Integer> {
}
